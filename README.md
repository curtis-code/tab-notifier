# Tab Notifier

## Install

```
npm install --save tab-notifier
```

## Options

 - `blink` (default: `false`)
 - `blinkSpeed` (default: `1000`)
 - `replaceTitle` (default: `false`)

## Development
### Build

```
npm run build
```